# quant_toolbox – Python dependencies
# Install with: pip install -r requirements.txt

# ── Data downloading ────────────────────────────────────────────────────────
ccxt>=4.3.0           # Unified crypto exchange API (REST + WebSocket via ccxt.pro)

# ── Data processing ─────────────────────────────────────────────────────────
pandas>=2.2.0         # DataFrame operations
pyarrow>=15.0.0       # Parquet read/write backend
numpy>=1.26.0         # Numerical computing

# ── Configuration & validation ──────────────────────────────────────────────
pydantic>=2.6.0       # Data validation and settings management
pydantic-settings>=2.2.0  # BaseSettings for environment-variable loading

# ── (Optional) Deep learning ────────────────────────────────────────────────
# Uncomment the framework you intend to use:
# torch>=2.2.0
# tensorflow>=2.16.0
# scikit-learn>=1.4.0

# ── Development / testing ───────────────────────────────────────────────────
pytest>=8.0.0
